const SectionTitle = ({ heading }) => {
  return (
    <div className="mb-8 text-center">
      <h3 className="text text-4xl uppercase">{heading}</h3>
    </div>
  );
};

export default SectionTitle;
